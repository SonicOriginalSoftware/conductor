syntax = "proto3";
option go_package = "./generated";

import "nil.proto";
import "job.proto";

service Runner {
  rpc Start(Job) returns (nil) {}
  rpc Stop(nil) returns (nil) {}
  rpc Status(nil) returns (RunnerStatus) {}
}

message RunnerStatus {
  string CurrentJobName = 1;
  string CurrentCommandName = 2;
}
